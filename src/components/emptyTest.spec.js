// Must have at least one test file in this directory or Mocha will throw an error.
// // Dummy test to supress error message in console

describe('Addition', () => {
  it('knows that 2 and 2 make 4', () => {
    expect(2 + 2).toBe(4);
  });
});
